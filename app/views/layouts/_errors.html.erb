<h3>
<% if obj.present? %> <!-- any > present コメント機能のエラーメッセの関係で変更している -->
  <%= obj.errors.count %>errors prohibited this obj from being saved:
</h3>
<ul>
  <% obj.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
</ul>
<% end %>